<div class="bom-salvage-choice">
  <h4>{{localize "BOM.aftermath.salvage"}} — Choose Benefit</h4>
  {{#each benefits}}
  <div class="bom-choice-option" data-benefit="{{this.id}}">
    <strong>{{localize this.label}}</strong>
    <span>{{localize this.desc}}</span>
  </div>
  {{/each}}
</div>
