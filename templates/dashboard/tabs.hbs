<nav class="bom-tabs sheet-tabs" data-group="sheet">
  {{#each @root.constructor.TABS.sheet.tabs}}
  <a class="item {{#if (eq ../tab this.id)}}active{{/if}}"
     data-tab="{{this.id}}" data-group="sheet">
    <i class="{{this.icon}}"></i> {{localize this.label}}
  </a>
  {{/each}}
</nav>
